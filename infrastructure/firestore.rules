rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access — backend handles all reads/writes
    // via Admin SDK which bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
